<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8"/>
  <meta content="IE=edge" http-equiv="X-UA-Compatible"/>
  <meta content="width=device-width, initial-scale=1, shrink-to-fit=no" name="viewport"/>
  <title><%= title %></title>
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
  
  <style>
    .error-container {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%);
    }
    
    .error-card {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      border-radius: 20px;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
    }
    
    .error-icon {
      animation: pulse 2s ease-in-out infinite alternate;
    }
    
    @keyframes pulse {
      0% { transform: scale(1); }
      100% { transform: scale(1.05); }
    }
    
    .btn-retry {
      background: linear-gradient(45deg, #ff6b6b, #ee5a24);
      border: none;
      border-radius: 25px;
      padding: 12px 30px;
      color: white;
      font-weight: 600;
      text-decoration: none;
      transition: all 0.3s ease;
    }
    
    .btn-retry:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(255, 107, 107, 0.4);
      color: white;
    }
  </style>
</head>
<body>
  <div class="error-container">
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-lg-6 col-md-8">
          <div class="error-card p-5 text-center">
            <!-- Icono de error -->
            <div class="error-icon mb-4">
              <i class="bi bi-exclamation-triangle-fill text-danger" style="font-size: 4rem;"></i>
            </div>
            
            <!-- Mensaje de error -->
            <h2 class="h3 text-dark mb-3">¡Ups! Algo salió mal</h2>
            <p class="text-muted mb-4">
              Se ha producido un error inesperado en el servidor. 
              Nuestro equipo técnico ha sido notificado y está trabajando para solucionarlo.
            </p>
            
            <!-- Detalles del error (solo en desarrollo) -->
            <% if (typeof error !== 'undefined' && process.env.NODE_ENV === 'development') { %>
              <div class="alert alert-danger text-start mb-4">
                <h6><i class="bi bi-bug me-2"></i>Detalles del Error (Desarrollo)</h6>
                <code class="small"><%= error %></code>
              </div>
            <% } %>
            
            <!-- ID del error para tracking -->
            <div class="mb-4">
              <small class="text-muted">
                <i class="bi bi-hash me-1"></i>
                ID del Error: <code><%= Math.random().toString(36).substr(2, 9).toUpperCase() %></code>
              </small>
            </div>
            
            <!-- Botones de acción -->
            <div class="mb-4">
              <button onclick="window.location.reload()" class="btn-retry me-3">
                <i class="bi bi-arrow-clockwise me-2"></i>Intentar de Nuevo
              </button>
              <a href="/" class="btn btn-outline-secondary">
                <i class="bi bi-house me-2"></i>Ir al Inicio
              </a>
            </div>
            
            <!-- Información adicional -->
            <div class="bg-light p-3 rounded">
              <h6 class="text-primary mb-2">
                <i class="bi bi-info-circle me-2"></i>¿Qué puedes hacer?
              </h6>
              <ul class="list-unstyled text-start mb-0">
                <li class="mb-2">
                  <i class="bi bi-check2 text-success me-2"></i>
                  Espera unos minutos e intenta nuevamente
                </li>
                <li class="mb-2">
                  <i class="bi bi-check2 text-success me-2"></i>
                  Verifica tu conexión a internet
                </li>
                <li class="mb-2">
                  <i class="bi bi-check2 text-success me-2"></i>
                  Si el problema persiste, contacta al soporte técnico
                </li>
              </ul>
            </div>
            
            <!-- Footer -->
            <div class="mt-4 pt-3 border-top">
              <small class="text-muted">
                <%= typeof appName !== 'undefined' ? appName : 'Obra 360' %> © <%= new Date().getFullYear() %>
                <span class="mx-2">•</span>
                <%= new Date().toLocaleString('es-AR') %>
              </small>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      console.log('❌ Página de error cargada');
      
      // Auto-retry después de 30 segundos (opcional)
      setTimeout(() => {
        if (confirm('¿Quieres intentar cargar la página nuevamente?')) {
          window.location.reload();
        }
      }, 30000);
    });
  </script>
</body>
</html>